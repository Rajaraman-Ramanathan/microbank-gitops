apiVersion: v1
kind: Service
metadata:
  name: postgres-auth-service
  labels:
    app: postgres-auth
spec:
  type: ClusterIP
  selector:
    app: postgres-auth
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
