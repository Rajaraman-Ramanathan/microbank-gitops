apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-postgres
  namespace: postgres
spec:
  podSelector: {}
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: microbank-dev
    ports:
    - protocol: TCP
      port: 5432
  policyTypes:
  - Ingress