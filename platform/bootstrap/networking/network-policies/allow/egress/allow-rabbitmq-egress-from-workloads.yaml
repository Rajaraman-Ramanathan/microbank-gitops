apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-rabbitmq-egress-from-workloads
  namespace: microbank-dev
  annotations:
    argocd.argoproj.io/sync-wave: "6"
spec:
  podSelector:
    matchLabels:
      network-role: backend

  policyTypes:
  - Egress

  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          tier: platform
      podSelector:
        matchLabels:
          network-role: platform
          app.kubernetes.io/name: rabbitmq
    ports:
    - protocol: TCP
      port: 5672