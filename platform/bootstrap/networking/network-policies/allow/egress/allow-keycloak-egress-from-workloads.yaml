apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-keycloak-egress-from-workloads
  namespace: microbank-dev
  annotations:
    argocd.argoproj.io/sync-wave: "6"
spec:
  podSelector:
    matchLabels:
      network-role: backend

  policyTypes:
  - Egress

  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          tier: identity
      podSelector:
        matchLabels:
          network-role: identity
          app.kubernetes.io/name: keycloak
    ports:
    - protocol: TCP
      port: 8080